# Codex AI Agent 编程实战：从需求文档到软件安装包统计系统

这次我用 Codex 完成了一次比较完整的 AI Agent 编程实战：从一份中文需求文档出发，逐步规划、实现、测试、构建，并最终交付出一个可以本地运行的 Web 应用。

项目叫 **Software Package Explorer**，中文名是“软件安装包统计系统”。它的核心能力是扫描指定目录中的软件安装包，自动解析软件名称、版本、平台、大小、更新时间、存储路径等信息，并以树形结构、卡片、表格、图表和导出文件的形式展示出来。

这篇文章不是单纯介绍一个小工具，而是记录一次真实的 AI Agent 编程过程：Codex 如何理解需求、如何拆解任务、如何写代码、如何发现问题、如何验证交付，以及我对这种开发方式的一些体会。

## 一、从一份需求文档开始

项目一开始只有一份需求文档：[软件安装包统计系统需求文档.md](./软件安装包统计系统需求文档.md)。

文档里描述了系统目标：

- 扫描指定目录中的软件安装包。
- 自动识别软件名称、版本、文件类型、平台、大小、更新时间和路径。
- 提供树形结构、卡片、表格等多种展示方式。
- 支持搜索、筛选、排序、分页。
- 提供 CSV、JSON、Markdown 导出。
- 支持 Light、Dark、Blue、Green 多主题。

这个需求看起来不算特别复杂，但它已经覆盖了一个完整应用的多个层面：

- 文件系统扫描。
- 信息解析。
- 数据统计。
- 前后端 API。
- 可视化界面。
- 导出能力。
- 测试和交付。

如果手写，从零开始搭项目、设计结构、写 API、做 UI、补测试，依然要花不少时间。于是我把需求文档交给 Codex，让它先进入规划阶段。

## 二、AI Agent 不是直接开写，而是先读环境

这次实战里一个很重要的感受是：靠谱的 AI Agent 不是一上来就写代码，而是先确认项目环境。

Codex 一开始做了几件事：

- 查看当前目录结构。
- 读取需求文档。
- 确认是否已有 Git 仓库。
- 检查 Node、npm、Python 等运行环境。
- 判断这是一个空项目，还是已有工程上的增量开发。

结果发现：当前目录里只有需求文档，没有现成代码，也不是 Git 仓库。也就是说，这不是“改一个已有项目”，而是“从零搭建一个 MVP”。

这个判断很关键。因为如果已有项目，Codex 应该遵循已有架构；如果是空项目，Codex 就需要负责技术选型和项目结构设计。

## 三、先规划，再实现

在正式写代码之前，Codex 先给出了一份实施计划。

技术选型是：

- 前端：React + TypeScript + Vite。
- 后端：Node.js + Express。
- 图表：Recharts。
- 图标：lucide-react。
- 测试：Node 内置测试运行器 + tsx。

应用形态是本地 Web 应用。也就是说，浏览器负责展示和交互，Node 后端负责读取本机目录并扫描文件。

这个选择很合理。因为浏览器本身不能随意读取用户本机任意目录，如果要支持用户输入类似 `/Users/name/software` 这样的路径，就需要本地服务端参与。

最终计划拆成几个模块：

```text
src/
  client/     前端 React 应用
  server/     Express API、扫描器、解析器、导出器
  shared/     前后端共享类型
```

同时规划了几个核心 API：

```text
POST /api/scan
POST /api/export
```

这个阶段并没有急着写代码，而是先把“要做什么、怎么做、怎么验收”说清楚。这一点对 AI Agent 编程非常重要。因为 Agent 一旦开始执行，如果方向没定好，后面会在错误路径上越走越远。

## 四、第一轮实现：完成可运行 MVP

确认方案后，Codex 开始从零搭项目。

它创建了 `package.json`、TypeScript 配置、Vite 配置，并实现了前后端代码。

后端主要包括：

- [parser.ts](./src/server/parser.ts)：解析文件类型、版本号、软件名和平台。
- [scanner.ts](./src/server/scanner.ts)：递归扫描目录，统计文件和目录。
- [exporter.ts](./src/server/exporter.ts)：导出 CSV、JSON、Markdown。
- [index.ts](./src/server/index.ts)：启动 Express 服务。

前端主要包括：

- [App.tsx](./src/client/App.tsx)：应用主界面和交互逻辑。
- [styles.css](./src/client/styles.css)：响应式布局、主题和组件样式。

共享类型放在：

- [types.ts](./src/shared/types.ts)

这一轮完成后，系统已经具备了完整 MVP 能力：

- 输入本机目录并扫描。
- 展示总览指标：软件总数、目录总数、总占用空间、平台数量。
- 展示分类树。
- 展示平台分布和空间占用图表。
- 提供卡片视图和表格视图。
- 支持搜索、平台过滤、文件类型过滤、大小范围、更新时间范围。
- 支持排序和分页。
- 支持四种主题切换。
- 支持 CSV、JSON、Markdown 导出。

这已经不是一个纯 Demo，而是一个可以实际跑起来的小工具。

## 五、测试暴露问题，Agent 继续修

AI 写代码最容易被质疑的一点是：它可能“一次看起来完成了”，但细节上有 bug。

这次 Codex 的表现比较有意思。它不是写完就结束，而是主动跑测试和构建。

第一轮测试时，确实暴露出几个问题：

- 文件名 `ChromeSetup_136.0.exe` 被解析成了 `ChromeSetup`，而不是 `Chrome`。
- `wechat_v3.9.10_x64.exe` 的 `x64` 清理不够准确。
- `Office2021.iso` 这种年份紧贴名称的版本号没有识别出来。
- 分类树里 Windows 节点出现重复。
- 生产启动路径和构建产物路径不完全匹配。

这些问题都不是需求文档里明确写了“怎么修”的，但它们都是实现一个真实可用工具时会遇到的细节。

Codex 的处理方式是：

1. 根据测试失败信息定位问题。
2. 修改解析规则。
3. 修正目录树节点匹配逻辑。
4. 调整生产启动路径。
5. 重新跑测试和构建。

最终第一轮交付时：

- `npm test` 全部通过。
- `npm run build` 通过。
- `npm audit --audit-level=moderate` 显示 0 vulnerabilities。
- 生产服务可以通过 `npm run start` 启动。
- API 冒烟测试正常。

这让我觉得，AI Agent 编程的关键不是“AI 一次生成完美代码”，而是“AI 能不能持续验证、发现问题、修正问题”。

## 六、第二轮加固：从能跑到更适合交付

第一轮完成后，我继续让 Codex 做交付加固。

这一轮主要做了三件事。

### 1. 增加项目文档和忽略规则

新增了：

- [.gitignore](./.gitignore)
- [README.md](./README.md)

README 里说明了：

- 如何安装和启动。
- 开发模式和生产模式的服务地址。
- API 用法。
- 支持的文件类型。
- 当前解析规则。
- MVP 限制。

这一步很重要。代码能跑是一回事，项目能不能被别人理解、接手和继续维护，是另一回事。

### 2. 重构 Express 启动逻辑

原本 Express app 创建和服务监听都在 `index.ts` 里。这样虽然能跑，但不方便写 API 集成测试。

Codex 将其拆成：

- [app.ts](./src/server/app.ts)：导出 `createApp()`。
- [index.ts](./src/server/index.ts)：负责创建 app 并启动监听。

这样测试可以直接创建 app，然后用临时端口启动，不需要启动一个长期运行的生产服务。

### 3. 增加 API 集成测试

新增了：

- [api.test.ts](./src/server/__tests__/api.test.ts)

测试覆盖：

- `POST /api/scan` 正常扫描临时目录。
- `POST /api/scan` 缺少路径时返回 400。
- `POST /api/export` 正常导出 CSV。
- `POST /api/export` 非法格式返回 400。

这一轮完成后，测试数量增加到 13 个：

```text
tests 13
pass 13
fail 0
```

## 七、一次完整的 Agent 工作流

回看整个过程，Codex 的工作流大致是这样的：

```text
读取需求
  ↓
检查项目环境
  ↓
生成实施计划
  ↓
搭建项目结构
  ↓
实现后端扫描、解析、导出
  ↓
实现前端 UI 和交互
  ↓
补单元测试
  ↓
运行测试和构建
  ↓
根据失败结果修复问题
  ↓
浏览器端到端验证
  ↓
补 README、.gitignore 和 API 集成测试
  ↓
再次测试、构建、审计、生产冒烟
```

这和传统开发流程很像，只是很多机械性的环节由 AI Agent 自动完成了。

我需要做的不是逐行写代码，而是：

- 提供清晰需求。
- 判断方案是否合理。
- 观察验证结果。
- 决定下一步优化方向。

从这个角度看，AI Agent 更像一个能主动执行的工程搭档，而不是一个只会回答问题的聊天机器人。

## 八、这次实战里 Codex 做得好的地方

### 1. 能根据空项目自动完成技术选型

当前目录只有需求文档，Codex 判断需要从零搭建，并选择了 React + TypeScript + Vite + Express 这一套轻量技术栈。

这个选择比较贴合 MVP：上手快、依赖少、易于本地运行。

### 2. 会主动验证，而不是只写代码

Codex 不只是生成文件，还主动执行：

```bash
npm test
npm run build
npm audit --audit-level=moderate
```

并且在测试失败时继续修复。

### 3. 能发现真实工程细节问题

例如：

- 树节点重复。
- 生产入口路径不匹配。
- Rollup 原生包在不同 Node 环境下的加载问题。
- npm audit 中开发依赖和生产依赖的区别。

这些都属于真实项目里常见但容易被忽略的细节。

### 4. 会把项目从“代码”推进到“交付”

第二轮加固中，Codex 补了 README、`.gitignore` 和 API 集成测试。这让项目不只是能跑，而是更像一个可以继续维护的工程。

## 九、AI Agent 编程仍然需要人的判断

虽然这次 Codex 完成度很高，但我也能明显感觉到，人仍然需要参与关键判断。

比如：

- 首版是做本地 Web 应用，还是静态报告生成器？
- 版本解析是先做文件名规则，还是直接深入 PE/MSI/APK 元数据？
- MVP 是优先功能完整，还是优先视觉打磨？
- 对构建告警是否要立即处理？
- 当前是否需要引入更多依赖？

这些不是纯代码问题，而是产品范围和工程取舍问题。

AI Agent 可以提出方案，也可以执行方案，但最终要不要这么做，仍然需要人来判断。

## 十、我的几点体会

### 1. 需求文档越清楚，Agent 越能发挥

这次能比较顺利，一个重要原因是需求文档本身写得比较完整。它明确了功能、展示方式、导出格式、主题和响应式要求。

AI Agent 很适合把结构化需求转化成工程实现。

### 2. 不要只让 AI 写代码，要让它验证代码

如果只停留在“帮我实现”，很容易得到一个看似完整但没验证过的项目。

更好的方式是明确要求：

- 跑测试。
- 跑构建。
- 做浏览器验证。
- 做生产冒烟。
- 说明失败原因和修复结果。

这次项目真正变得可靠，是从测试和构建开始的。

### 3. 让 Agent 分阶段推进，效果更好

一次性要求“完成全部需求”容易范围过大。更好的节奏是：

1. 先完成 MVP。
2. 再做交付加固。
3. 后续继续增强功能。

这样每一轮都有明确目标，也更容易验收。

### 4. AI Agent 更适合“有反馈的持续迭代”

这次最有价值的地方不是 Codex 第一次写了多少代码，而是它能在发现问题后继续调整。

测试失败、构建失败、路径问题、环境问题，都可以成为下一轮输入。Agent 能把这些反馈转化成具体修复。

## 十一、后续可以继续增强什么

这个项目现在已经是一个可运行 MVP，但后续还有很多增强方向：

- 增加 PE、MSI、APK 元数据深度解析。
- 支持扫描进度和取消任务。
- 支持大目录增量扫描。
- 增加重复文件检测。
- 增加按软件维度聚合多平台版本。
- 支持保存历史扫描结果。
- 增加更完整的前端组件测试或端到端测试。
- 优化图表库打包体积。
- 做成 Electron 或 Tauri 桌面应用。

这些都可以继续交给 Codex 分阶段完成。

## 结语

这次 Codex AI Agent 编程实战，让我更直观地感受到：AI 编程工具正在从“代码补全”走向“工程协作”。

它能读需求、看环境、定方案、写代码、跑测试、修 bug、补文档、做交付检查。对开发者来说，角色也在变化：我们不再只是代码的逐行输入者，而更像是需求定义者、方向把关者和质量验收者。

当然，AI Agent 不是万能的。它仍然需要清晰的需求、明确的边界和人的工程判断。但如果使用得当，它确实可以把一个想法更快地推进成一个可运行、可验证、可继续迭代的产品。

这次的软件安装包统计系统只是一个开始。后续我会继续基于这个项目，探索如何用 Codex 进一步优化功能、增强体验，并把 AI Agent 编程变成更加稳定、可复用的开发工作流。
